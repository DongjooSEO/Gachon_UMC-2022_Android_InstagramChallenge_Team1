<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/shop_search_back_iv"
        android:layout_width="25dp"
        android:layout_height="30dp"
        android:scaleType="fitCenter"
        android:src="@drawable/shop_search_back"
        android:layout_marginTop="60dp"
        android:layout_marginStart="15dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <EditText
        android:id="@+id/shop_search_search_et"
        android:layout_width="340dp"
        android:layout_height="wrap_content"
        android:background="@drawable/rounding"
        android:hint="Shop 검색"
        android:inputType="text"
        android:textSize="15sp"
        android:textColor="@color/black"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="15dp"
        app:layout_constraintStart_toEndOf="@id/shop_search_back_iv"
        app:layout_constraintTop_toTopOf="@id/shop_search_back_iv"
        app:layout_constraintBottom_toBottomOf="@id/shop_search_back_iv">

        <requestFocus/>

    </EditText>

</androidx.constraintlayout.widget.ConstraintLayout>